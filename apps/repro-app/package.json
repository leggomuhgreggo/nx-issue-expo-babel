{
  "name": "repro-app",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "react-native": "*"
  },
  "scripts": {
    "postinstall": "rm -r node_modules && cd ../../ && yarn install && npx nx sync-deps repro-app && npx nx ensure-symlink repro-app"
  }
}
